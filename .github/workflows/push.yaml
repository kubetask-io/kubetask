# Copyright Contributors to the KubeOpenCode project
name: Push

on:
  push:
    branches:
      - main
    paths-ignore:
      - "docs/**"
      - "*.md"
      - "**/*.md"
      - "LICENSE"
      - ".gitignore"

env:
  GO_VERSION: "1.25"
  IMG_REGISTRY: quay.io
  IMG_ORG: kubeopencode
  IMG_NAME: kubeopencode

jobs:
  # Detect which paths changed to conditionally build images
  changes:
    name: Detect Changes
    runs-on: ubuntu-latest
    outputs:
      kubeopencode: ${{ steps.filter.outputs.kubeopencode }}
      agent-opencode: ${{ steps.filter.outputs.agent-opencode }}
      agent-devbox: ${{ steps.filter.outputs.agent-devbox }}
      agent-code-server: ${{ steps.filter.outputs.agent-code-server }}
      helm: ${{ steps.filter.outputs.helm }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Check for changes
        uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            kubeopencode:
              - 'cmd/kubeopencode/**'
              - 'internal/**'
              - 'api/**'
              - 'go.mod'
              - 'go.sum'
              - 'Dockerfile'
            agent-opencode:
              - 'agents/opencode/**'
            agent-devbox:
              - 'agents/devbox/**'
            agent-code-server:
              - 'agents/code-server/**'
            helm:
              - 'charts/kubeopencode/**'

  unit-test:
    name: Unit Tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}

      - name: Run unit tests
        run: make test

  integration-test:
    name: Integration Tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}

      - name: Run integration tests
        run: make integration-test

  e2e-test:
    name: E2E Tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}

      - name: Install Kind
        run: |
          curl -Lo ./kind https://kind.sigs.k8s.io/dl/v0.24.0/kind-linux-amd64
          chmod +x ./kind
          sudo mv ./kind /usr/local/bin/kind

      - name: Install Helm
        run: |
          curl https://raw.githubusercontent.com/helm/helm/main/scripts/get-helm-3 | bash

      - name: Run e2e tests
        run: make e2e

  # Build and push kubeopencode image (only when kubeopencode code changes)
  build-kubeopencode:
    name: Build KubeOpenCode
    runs-on: ubuntu-latest
    needs: [changes, unit-test, integration-test, e2e-test]
    if: needs.changes.outputs.kubeopencode == 'true'
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Quay.io
        uses: docker/login-action@v3
        with:
          registry: quay.io
          username: ${{ secrets.QUAY_ROBOT_ACCOUNT }}
          password: ${{ secrets.QUAY_TOKEN }}

      - name: Build and push kubeopencode
        uses: docker/build-push-action@v6
        with:
          context: .
          platforms: linux/amd64,linux/arm64
          push: true
          tags: |
            ${{ env.IMG_REGISTRY }}/${{ env.IMG_ORG }}/${{ env.IMG_NAME }}:latest
            ${{ env.IMG_REGISTRY }}/${{ env.IMG_ORG }}/${{ env.IMG_NAME }}:${{ github.sha }}

  # Build and push opencode agent (only when agents/opencode changes)
  build-agent-opencode:
    name: Build Agent OpenCode
    runs-on: ubuntu-latest
    needs: [changes, unit-test, integration-test, e2e-test]
    if: needs.changes.outputs.agent-opencode == 'true'
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Quay.io
        uses: docker/login-action@v3
        with:
          registry: quay.io
          username: ${{ secrets.QUAY_ROBOT_ACCOUNT }}
          password: ${{ secrets.QUAY_TOKEN }}

      - name: Build and push opencode agent
        uses: docker/build-push-action@v6
        with:
          context: ./agents/opencode
          platforms: linux/amd64,linux/arm64
          push: true
          tags: |
            ${{ env.IMG_REGISTRY }}/${{ env.IMG_ORG }}/kubeopencode-agent-opencode:latest
            ${{ env.IMG_REGISTRY }}/${{ env.IMG_ORG }}/kubeopencode-agent-opencode:${{ github.sha }}

  # Build and push devbox agent (only when agents/devbox changes)
  build-agent-devbox:
    name: Build Agent Devbox
    runs-on: ubuntu-latest
    needs: [changes, unit-test, integration-test, e2e-test]
    if: needs.changes.outputs.agent-devbox == 'true'
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Quay.io
        uses: docker/login-action@v3
        with:
          registry: quay.io
          username: ${{ secrets.QUAY_ROBOT_ACCOUNT }}
          password: ${{ secrets.QUAY_TOKEN }}

      - name: Build and push devbox agent
        uses: docker/build-push-action@v6
        with:
          context: ./agents/devbox
          platforms: linux/amd64,linux/arm64
          push: true
          tags: |
            ${{ env.IMG_REGISTRY }}/${{ env.IMG_ORG }}/kubeopencode-agent-devbox:latest
            ${{ env.IMG_REGISTRY }}/${{ env.IMG_ORG }}/kubeopencode-agent-devbox:${{ github.sha }}

  # Build and push code-server agent (only when agents/code-server or devbox changes)
  build-agent-code-server:
    name: Build Agent Code-Server
    runs-on: ubuntu-latest
    needs: [changes, unit-test, integration-test, e2e-test]
    if: needs.changes.outputs.agent-code-server == 'true' || needs.changes.outputs.agent-devbox == 'true'
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Quay.io
        uses: docker/login-action@v3
        with:
          registry: quay.io
          username: ${{ secrets.QUAY_ROBOT_ACCOUNT }}
          password: ${{ secrets.QUAY_TOKEN }}

      - name: Build and push code-server agent
        uses: docker/build-push-action@v6
        with:
          context: ./agents/code-server
          platforms: linux/amd64,linux/arm64
          push: true
          tags: |
            ${{ env.IMG_REGISTRY }}/${{ env.IMG_ORG }}/kubeopencode-agent-code-server:latest
            ${{ env.IMG_REGISTRY }}/${{ env.IMG_ORG }}/kubeopencode-agent-code-server:${{ github.sha }}

  # Push Helm chart (only when chart changes)
  push-helm-chart:
    name: Push Helm Chart
    runs-on: ubuntu-latest
    needs: [changes, unit-test, integration-test, e2e-test]
    if: needs.changes.outputs.helm == 'true'
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Helm
        run: |
          curl https://raw.githubusercontent.com/helm/helm/main/scripts/get-helm-3 | bash

      - name: Login to Helm registry
        run: |
          echo "${{ secrets.QUAY_TOKEN }}" | helm registry login quay.io -u ${{ secrets.QUAY_ROBOT_ACCOUNT }} --password-stdin

      - name: Package and push Helm chart
        run: |
          mkdir -p dist
          helm package charts/kubeopencode -d dist/
          helm push dist/kubeopencode-*.tgz oci://quay.io/kubeopencode/helm-charts
