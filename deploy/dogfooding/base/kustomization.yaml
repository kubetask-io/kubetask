# Copyright Contributors to the KubeOpenCode project
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: kubeopencode-dogfooding

resources:
  - namespace.yaml
  - rbac.yaml
  - secrets.yaml
  - kubeopencodeconfig.yaml
  - agent-bot.yaml
  - agent-dev.yaml
  - agent-refactor.yaml
  - agent-slack.yaml
  - context-bot-instructions.yaml
  - context-dev-instructions.yaml
  - context-refactor-instructions.yaml
  - context-slack-instructions.yaml

# Note: Webhook functionality has been migrated to Argo Events.
# See ../github/ for EventSource and Sensor configuration.
# See ../scheduled/ for CronWorkflow configuration.
