# Copyright Contributors to the KubeOpenCode project
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: kubeopencode-dogfooding

resources:
  - namespace.yaml
  - rbac.yaml
  - secrets.yaml
  - kubeopencodeconfig.yaml
  - agent-default.yaml
  - configmap-github-scripts.yaml

# Note: Webhook functionality has been migrated to Argo Events.
# See ../argo-events/ for EventSource and Sensor configuration.
# See ../system/ for supporting infrastructure (smee-client, route).
